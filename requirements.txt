# Current MVP — uses Python stdlib only (no pip installs needed)
# Uncomment as you implement each phase:

# Phase 1 — Auth + Payments
# supabase==2.10.0
# stripe==10.12.0
# python-dotenv==1.0.1

# Phase 2 — Progress tracking
# Pillow==10.3.0
