# English Buddy — Python dependencies
# Phase 1 (current): stdlib only — no pip installs needed
# server.py uses only: http.server, urllib, json, os, sys

# Optional — only needed for local development with .env file
# Render sets env vars directly in dashboard so dotenv not needed there
python-dotenv==1.0.1

# Uncomment when implementing Phase 1 auth + payments:
# supabase==2.10.0
# stripe==10.12.0
